// RecordingAttributes is defined in Intents/SharedTypes.swift so it can be
// shared between the main app target and the widget extension target.
// That file must be added to both targets in Xcode for Live Activities to work.
